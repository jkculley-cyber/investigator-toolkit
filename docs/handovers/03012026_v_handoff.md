# Handover — Session V
**Date:** 2026-03-01
**Focus:** Demo seed data (Navigator + Meridian) + Product Hub + Command Center URL

---

## What Was Done

### 1. Navigator Demo Seed Data (`supabase/seed_navigator.mjs`) — NEW
Seeded 13 referrals, 6 placements, 6 supports for Lone Star ISD. Also enabled `navigator` product for Lone Star ISD.

**Risk scenarios seeded:**
| Student | Risk | Triggers |
|---------|------|---------|
| Marcus Johnson | HIGH | 3 referrals ≤14d + OSS |
| DeShawn Williams | HIGH | OSS + DAEP escalation |
| Jayden Martinez | HIGH | Prior DAEP placement |
| David Chen | MEDIUM | 2 referrals + ISS |
| Tyler Brown | MEDIUM | SPED + ISS |
| Aaliyah Davis | MEDIUM | ISS + multiple supports |
| Carlos Reyes | LOW | 1 old referral |
| Isabella Thompson | LOW | Support working, improving |

Seeder uses Supabase REST API with service role key — no DB password needed. Run with:
```
SUPABASE_SERVICE_ROLE_KEY=xxx node supabase/seed_navigator.mjs
```

---

### 2. Meridian Demo Seed Data (`supabase/seed_meridian.mjs`) — PORTED to REST API
Ported from direct pg connection to Supabase REST API (same approach as seed_navigator.mjs). Also enabled `meridian` product for Lone Star ISD.

**Data seeded:**
- 9 meridian students (range of disability categories: autism, LD, ED, deaf-blind, OHI)
- 4 IEPs (active and expiring)
- 2 504 plans
- 3 ARD referrals
- 1 CAP finding with 4 tasks

Run with:
```
SUPABASE_SERVICE_ROLE_KEY=xxx node supabase/seed_meridian.mjs
```

---

### 3. Product Hub Tab in Command Center (`src/pages/WaypointAdminPage.jsx`) — NEW TAB
Added a third "Product Hub" tab at `/waypoint-admin`. Contains:
- **4 Product Cards** (Waypoint/orange, Navigator/blue, Meridian/purple, Origins/teal) — color band, tagline, description, quick links to all key pages
- **Demo Sites** — 8 external link tiles (Staff Login, Parent Portal, DAEP Kiosk, Orientation Kiosk, Family Portal, Marketing Site, Whitepaper, Waypoint Admin)
- **Demo Credentials** — all 10 demo accounts in groups (Lone Star ISD Staff × 7, Parent Portal × 2, Internal × 1) with show/hide passwords toggle and one-click copy for email + password

---

### 4. Command Center Header URL Pill (`src/pages/WaypointAdminPage.jsx`)
Added a URL display pill to the Command Center header (visible on `sm+` screens, hidden on mobile):
- Shows `https://waypoint.clearpathedgroup.com/waypoint-admin`
- **Copy button** — copies URL to clipboard with green checkmark feedback
- **Open button** — opens in new tab

Constant: `COMMAND_CENTER_URL = 'https://waypoint.clearpathedgroup.com/waypoint-admin'`

---

## What's Next

### Immediate — Pending Migrations
1. **Apply migration 050** — `supabase/migrations/050_navigator_intelligence.sql`. Adds `skill_gap` + `skill_gap_notes` to `navigator_referrals`; `outcome_notes`, `incidents_before`, `incidents_after` to `navigator_supports`. Required for Skill Gap Map + Effectiveness pages to show real data.
2. **Apply migration 049** — `supabase/migrations/049_meridian_transition_rda.sql`. Creates `meridian_secondary_transitions`, `meridian_rda_determination`, `meridian_rda_indicators`. Required for Transition SPPI-13 and RDA Dashboard pages to show real data.
3. **Apply migration 044** — Origins DB schema.

### Form Wiring (after migration 050)
4. **Wire `skill_gap` field into Navigator Referral form** — A `<select>` with the 6 `SKILL_GAP_LABELS` values.
5. **Wire effectiveness fields into Navigator Supports edit modal** — `incidents_before`, `incidents_after`, `outcome_notes` (shown when `status = 'completed'`).

### Still Pending (Carried)
- Enable Origins for Lone Star ISD (needs migration 044 first)
- Set up `privacy@clearpathedgroup.com`
- Resend sender domain verification (`waypointdaep.com`)
- Supabase redirect URL for reset-password
- Google Search Console registration
- Supabase Pro upgrade
- First pilot district

---

## Key Files

| Path | Notes |
|------|-------|
| `supabase/seed_navigator.mjs` | NEW — 13 referrals, 6 placements, 6 supports; Navigator enabled for Lone Star ISD |
| `supabase/seed_meridian.mjs` | PORTED to REST API — 9 students, 4 IEPs, 2 504 plans; Meridian enabled for Lone Star ISD |
| `src/pages/WaypointAdminPage.jsx` | +Product Hub tab, +header URL pill with copy/open |
