# Handover — Session S
**Date:** 2026-02-28
**Focus:** Meridian SPPI-13 Secondary Transition Tracker + RDA Dashboard

---

## What Was Done

### Full implementation of two new Meridian pages per the pre-planned spec:

#### Migration 049 (`supabase/migrations/049_meridian_transition_rda.sql`)
- **NOT yet applied to production** — paste SQL into SQL Editor before demoing these features
- `meridian_secondary_transitions` — one row per student per school year; 5 SPPI-13 elements (postsecondary goals, transition assessments, transition services, student participation, agency involvement)
- `meridian_rda_determination` — one row per district per school year; DL1–DL4 + SSP due/submitted dates + check-in cadence
- `meridian_rda_indicators` — one row per SPPI indicator per district per school year
- All 3 tables have RLS: `district_id = user_district_id() OR is_waypoint_admin()`

#### `src/hooks/useMeridian.js` additions
- `currentSchoolYear()` helper (August cutoff: month >= 7)
- `useSPPI13Students()` — grade ≥ 10 + sped_status = 'eligible', joins transitions + IEPs
- `useRDADetermination()` — single row for current school year, maybeSingle()
- `useRDAIndicators()` — all indicators for current school year
- `upsertTransitionPlan(data)` — UPSERT on `district_id,student_id,school_year`
- `upsertRDADetermination(data)` — UPSERT with auto `checkin_cadence_days` (dl2→90, dl3→60, dl4→30)
- `upsertRDAIndicator(data)` — UPSERT on `district_id,school_year,sppi_number`

#### `src/pages/meridian/MeridianSPPI13Page.jsx` (NEW)
- Header with IDEA § 300.320 badge + "Generate Report" button (jsPDF)
- 4 stat cards: Requires Plan / Fully Compliant / Needs Attention / Compliance %
- FilterTabs: All | Compliant | Needs Attention | Missing Plan
- Table: ✓/✕/— per element column, Status badge (complete/pending/overdue for grade 12+), "Update →" action
- Empty state when no grade 10+ eligible students
- **TransitionPlanModal**: 5 accordion sections, one per SPPI-13 element; upserts on save
- **jsPDF Report**: district header, compliance rate, autoTable with all students, noncompliant summary with missing elements listed
- Grade ≥ 10 used as proxy for age 16+ (noted in UI)

#### `src/pages/meridian/MeridianRDAPage.jsx` (NEW)
- Header with TEA badge + "Update RDA Data" button
- **DL Banner**: color-coded by level (emerald/amber/orange/red); gray "no determination" state; SSP status + next check-in countdown for DL2+
- **Domain I**: SPPI-1 (Graduation Rate), SPPI-2 (Dropout Rate)
- **Domain II**: SPPI-5, 6, 7, 8, SPPI-11 (Live auto from `meridian_compliance_deadlines`), SPPI-12, SPPI-13 (Live auto from `meridian_secondary_transitions`)
- **Domain III**: SPPI-9, SPPI-10
- Each indicator card: number, label, district % vs state target, status pill (meets/approaching/not_meets), Live or Manual badge, Edit link for manual ones
- **RDADataModal** (3 steps): Step 1 Determination (DL radio + school year + date), Step 2 SSP Dates (DL2+ only, auto-computes next check-in), Step 3 SPPI Scores (manual indicators, SPPI-11/13 read-only Live)
- **IndicatorEditModal**: per-indicator edit (district %, state target, status, notes)

#### `src/App.jsx`
- +2 imports, +2 routes inside Meridian RequireProduct block:
  - `/meridian/transition` → `MeridianSPPI13Page`
  - `/meridian/rda` → `MeridianRDAPage`

#### `src/components/layout/Sidebar.jsx`
- `meridianNavigation` array updated: "Transition (SPPI-13)" + "RDA Dashboard" entries (between CAP Tracker and Waypoint Sync)
- `TransitionIcon` SVG (graduation cap)
- `RDAIcon` SVG (bar chart)

#### `DECISIONS.md`
- 3 decisions logged: migration 049, grade ≥ 10 proxy for age 16+, SPPI-11/13 auto-calculation sources

---

## Build Status
✅ `npm run build` — clean, 6.81s. Chunk size warning is pre-existing/expected.

---

## What's Next

### Immediate (before Meridian demo)
1. **Apply migration 049** — paste `supabase/migrations/049_meridian_transition_rda.sql` into [SQL Editor](https://supabase.com/dashboard/project/kvxecksvkimcgwhxxyhw/sql/new). Until then, pages show empty states.
2. **Enable Meridian for Lone Star ISD** — `/waypoint-admin` → Manage Lone Star ISD → Licensed Products → check Meridian.
3. **Seed Meridian demo data** — no SPED students exist yet. Need at minimum 3–4 grade 10–12 students in `meridian_students` with `sped_status = 'eligible'` and a campus assignment for SPPI-13 to show anything.

### Remaining Pending (carried from previous sessions)
- Apply migration 044 (Origins DB schema)
- Set up `privacy@clearpathedgroup.com`
- Verify Business Dashboard on live site
- Seed Meridian demo data
- Resend sender domain verification
- Supabase redirect URL for reset-password
- Google Search Console registration
- Supabase Pro upgrade (HaveIBeenPwned password protection)
- Meridian escalations table (separate future migration — escalate button logs to console only)
- First pilot district

---

## Key Files Reference

| Path | Notes |
|------|-------|
| `supabase/migrations/049_meridian_transition_rda.sql` | **Apply to production before demo** |
| `src/hooks/useMeridian.js` | 6 new exports at bottom of mutations section |
| `src/pages/meridian/MeridianSPPI13Page.jsx` | New — ~280 lines |
| `src/pages/meridian/MeridianRDAPage.jsx` | New — ~420 lines |
| `src/App.jsx` | Lines 46–47 (imports), lines 157–158 (routes) |
| `src/components/layout/Sidebar.jsx` | Lines 34–35 (nav items), bottom of file (icons) |

---

## Notes for Next Session
- `useDeadlines()` is imported in `MeridianRDAPage` for SPPI-11 auto-calculation. It filters on `evaluation_type` fields. Verify those field names match the `meridian_compliance_deadlines` view columns if SPPI-11 shows null unexpectedly.
- SPPI-13 "Live" value only shows a number once both: (a) migration 049 is applied AND (b) at least one student has a transition plan record. Before that it returns null and shows "—".
- The `useSPPI13Students` hook uses `.gte('grade', 10)` — this requires `grade` to be stored as a numeric-comparable type (TEXT that sorts lexicographically as '10','11','12' works; verify grade column type if no results appear for known grade 10+ students).
